# CI repair receipt

Branch: `fix/ci-db-seeding`

This branch adds an LFS-safe GitHub Actions checkout, deploy-surface pointer validation, Python compilation, deterministic SQLite seeding, database integrity assertions, row-count assertions, artifact upload, concurrency cancellation, and a 15-minute job timeout.
