# MirrorLease Finder Extension

This Finder Sync extension contributes **Share with AI — MirrorLease** directly
to the contextual menu for files in Desktop, Documents, and Downloads. It
launches the local MirrorLease host with only the selected file paths.

The watched scope is intentionally limited to those three folders. Monitoring
the entire home directory would create an unnecessarily broad Finder watcher.

